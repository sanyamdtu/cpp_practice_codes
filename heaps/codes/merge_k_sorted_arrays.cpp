#include<bits/stdc++.h>
using namespace std;
vector<int> merge(vector<int> v1.vetor<int> v2,vector<int> v3){
    
}
int main(){
    vector<int> v1,v2,v3;
    for(int i=0;i<3;i++)
        cin>>v1[i];
    for(int i=0;i<3;i++)
        cin>>v2[i];
    for(int i=0;i<3;i++)
        cin>>v3[i];
    return 0;
}